<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="css/recipe-search.css" />
    <title>Basic Eats</title>
</head>

<body>

    <div class="nav-bar">
        <a href="/">
            <img class="logo" src="images/logo.png" alt="" href="/index" />
        </a>
        <a href="/login" class="login">LOG IN/ SIGN UP</a>
        <div class="top-bar">
            <a href="/recipe-search" class="meal">MEAL</a>
            <p class="ingredient">INGREDIENT</p>
            <a href="/calendar" class="meal-plan">MEAL-PLAN</a>
            <a href="/aboutus" class="about-us">ABOUT US</a>
            <div class="search_bar">
                <form action="/search_recipes" method="get">
                    <div class="row">
                        <div id="custom-search-input">
                            <div class="input-group col-md-12">
                                <input type="text" id="searchValue" name="searchValue"
                                    class=" search-query form-control" placeholder="Search Recipes..." />
                                <span class="input-group-btn">
                                    <button class="btn btn-danger" type="submit">
                                        <span class=" glyphicon glyphicon-search"></span>
                                    </button>
                                </span>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <a href="/view-users" class="account">ACCOUNT</a>
        </div>
    </div>
    <h1 class="filter-text">
        <h1>Search Results for "<%= searchValue %>"</h1>
        <div class="results">
            <ul>
                <div class="row">
                    <% searchData.results.forEach((result, index) => { %>
                        <div class="col-5">
                            <img class="<%= index % 2 === 0 ? 'result-even' : 'result-odd' %>" src="<%= result.image %>" alt="" />
                            <p class="result-text"><%= result.title %></p>
                        </div>
                        <% if (index % 2 !== 0) { %>
                            </div><div class="row">
                        <% } %>

                </div>
                
                    </div>
                <% }) %>
                
                
            </ul>
</body>

<!-- search_results.ejs