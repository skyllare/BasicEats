<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link href="//netdna.bootstrapcdn.com/bootstrap/3.1.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.0/js/bootstrap.min.js"></script>
    <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="./css/account.css" />
    <title>My Account</title>
</head>

<body>
    <%- include('account_header', { admin: user.admin }) %>
    <% if(locals.msg && locals.msg==="password_success" ) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <p class="">Password successfully changed</p>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>
    <div class="container">
        <div class="row">
            <div class="col-sm-4">
                <p class="header mb-0">Username</p>
            </div>
            <div class="col-sm-8">
                <p class="answer mb-0">
                    <%= user.username %>
                </p>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-sm-4">
                <p class="header mb-0">Recipes Created</p>
            </div>
            <div class="col-sm-8">
                <p class="answer mb-0">
                    <%= user.recipe_count %>
                </p>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-sm-4">
                <p class="header mb-0">User Role</p>
            </div>
            <div class="col-sm-8">
                <% if(user.admin) { %>
                    <p class="answer mb-0">Admin</p>
                <% } else  { %>
                    <p class="answer mb-0">General</p>
                <% } %>
            </div>
        </div>
        <hr>
        <div class="container">
            <div class="row justify-content-center">
              <div class="col-md-6 text-center">
                <a href="/account/change_password">
                    <button>CHANGE PASSWORD</button>
                </a>
              </div>
            </div>
        </div>
    </div>
</body>